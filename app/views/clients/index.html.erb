<%= render "shared/notify_message" %>
<div class="row">
  <div class="col-md-12">
    <div class="portlet box light-grey">
      <div class="portlet-title">
        <div class="caption"><i class="fa fa-cogs"></i>客户列表</div>
        <div class="actions">
          <%= link_to "添加", new_client_path, :class => "btn default" %>
        </div>
      </div>
      <div class="portlet-body">
        <div class="dataTables_wrapper form-inline" role="grid">
          <div class="row">
            <div class="col-md-12 col-sm-12">
              <%= form_tag clients_path, :method => 'get' do %>
              <div class="dataTables_filter pull-left">
                <label>
                  关键字:<%= text_field_tag :search, params[:search], :class => "form-control input-small" %>
                </label>
                <%= submit_tag '查询', :name => nil, :class => 'btn' %>
              </div>
              <% end %>
            </div>
          </div>
          <div class="table-scrollable">
            <table class="table table-hover table-bordered">
              <thead>
                <tr>
                  <th>编号</th>
                  <th>名称</th>
                  <th>联系人</th>
                  <th>电话</th>
                  <th>传真</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
              <% @clients.each do |f| %>
                <tr>
                  <td><%= f.no %></td>
                  <td><%= f.name %></td>
                  <td><%= f.contact %></td>
                  <td><%= f.telephone %></td>
                  <td><%= f.fax %></td>
                  <td>
                    <%= link_to '编辑', edit_client_path(f) %>
                  </td>
                </tr>
              <% end %>
              </tbody>
            </table>
            <%= paginate @clients %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>